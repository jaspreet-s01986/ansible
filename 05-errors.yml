- name: Handling Errors
  hosts: all
  tasks:
  - name: Check if tomcat is running or not
    shell: uptime
    register: OUT
  - name: Printing output of uptime
    debug:
      msg: "{{ OUT.stdout }}"